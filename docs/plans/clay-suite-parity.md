# Clay Suite Parity Roadmap

Updated: 2026-09-13

OpenGTM should pursue workflow parity, not imitate every Clay screen. The target is a credible, self-hosted GTM operating system whose core loop—discover, enrich, segment, act, learn—works end to end.

## Current position

| Capability | OpenGTM today | Parity target | Priority |
| --- | --- | --- | --- |
| Tables and enrichment | Strong workbook, waterfalls, formulas, AI/research and output columns; reactive dependency controls; global saved-view filter/sort/search before pagination; matching-query run/fill and cost estimates; formula-safe full-query and ID-scoped streaming export; count-locked delete-all-matching; persistent ID-based cross-page selection; versioned stable keyset cursors for default and custom saved-view ordering; PostgreSQL trigram indexes for literal JSON search and compound expression indexes for common saved-view sorts; a self-cleaning controlled million-row gate for stable paging, indexed search, custom sorts, and exact distant-page selection; server-paged virtualized keyboard navigation; Shift-range copy and fill-down; atomic multi-row TSV paste; horizontally safe queue with bounded parallel slots, indexed tenant ownership, advisory-lock-enforced tenant backpressure/fair claiming, graceful drain, retry-bounded heartbeat recovery with domain reconciliation, admin telemetry, and a repeatable controlled-load gate | Execute and archive the workbook and queue gates on PostgreSQL at supported scale, extend expression indexes from common fields using production query telemetry, and broaden provider coverage | P0 |
| Discovery | Multi-source collection, provenance, canonical entities | More verified providers and repeatable source quality | P0 |
| Audiences | Persistent workspace audiences, materialized membership, stable indexed keyset traversal, entry/exit history, restart-safe scheduled refresh with parent-worker crash/timeout health reconciliation and failure-safe recurrence, live diffs, destinations, profile timelines and bounded account-level coverage/intent rollups | Controlled-live destination validation | P0 |
| Signals | Watches with stable indexed subscription and signal-feed traversal, intent polling with retry/backoff and parent-worker crash/timeout reconciliation for fetch, billing, and process failures; automation triggers, audience entry/exit triggers, unified profile timelines, tenant-safe volume/momentum/source/account analytics; and fail-closed signed source maturity | Controlled-live certifications for JobSpy, SEC EDGAR, website, technology and news sources | P1 |
| Activation | Durable audience destination runs and delivery ledger for webhooks, Slack, HubSpot, Salesforce, Instantly, Smartlead, idempotent-key Google Sheets upserts, atomic Airtable upserts, checksum-manifested JSONL warehouse ingestion and paid media; parent-worker crash/timeout reconciliation for retrying and terminal runs; cooperative cancellation at delivery and batch boundaries; in-place failed-delivery retries that preserve successful idempotency keys and run lineage; aggregate destination health, bounded recent failure evidence, and stable indexed cursor traversal for runs, deliveries, and inbound receipts; authenticated idempotent CRM inbound reconciliation with conflict policies; outreach/output columns; fail-closed signed support-maturity catalog | Controlled-live Slack, CRM, sequencer, Sheets, Airtable, and warehouse certifications plus additional destination breadth | P1 |
| Ads | Consent-gated, SHA-256 hashed add/remove reconciliation to Meta Custom Audiences, Google Customer Match and LinkedIn Matched Audiences; privacy-safe hashed delivery state, per-profile invalid-identifier skips, durable retry/idempotency, Google and LinkedIn partial-failure fail-closed batch accounting, and fail-closed signed support maturity | Controlled-live certification against approved platform accounts | P2 |
| Agents | Agent/research columns, MCP and Copilot surfaces; versioned chained research playbooks with prior-step context, an Agents composer, bounded audience runs, stable indexed run/result traversal, durable per-profile results, parent-worker crash/timeout reconciliation that preserves completed profiles, cooperative cancellation at profile/step boundaries, in-place failed or cancelled-profile retries that preserve successful work and frozen prompts, restart-safe recurring schedules, and signed capability maturity | Controlled-live certification of grounded research, chaining, audience runs, cancellation/retries, and schedules | P1 |
| Governance | Workspace member lifecycle and capability policies; RLS-backed tenant LLM usage accounting/reporting across SQLite and PostgreSQL; append-only mutation audit with stable indexed cursor inspection, bounded secret-stripping action metadata and complete filtered streaming export; crash-reconciled, legal-hold-aware retention covering operational and LLM-usage history; tenant-bound OIDC SSO; workspace-scoped SCIM 2.0 User and Group synchronization with hashed, expiring, rotatable tokens and last-use telemetry; expiring, revocable, last-use-tracked CRM inbound credentials; audited workspace-scoped credentials across CRM, sequencer, Airtable and Google Sheets outputs | Complete controlled-live IdP, directory, and connector validation | P2 |
| Ecosystem | Versioned YAML connector SDK, deterministic `.ogc` bundles, Ed25519 detached signatures, fail-closed installer with pairwise rollback on partial replacement, publisher trust store, runtime trust policy, CI review artifacts, BYOK providers, and a workspace-aware catalog that separates publisher trust from exact-build-bound, signed subject-specific controlled-live evidence contracts | Broader controlled-live certified community catalog | P2 |

## Ordered rollout

1. **Prove the core.** Complete ten consecutive HMAC-attested controlled-live gauntlet passes, publish a tagged release, and keep migrations plus RLS release-blocking.
2. **Make Audiences the shared object.** Persist filter definitions, refresh membership, record profile activity, and emit membership-change events.
3. **Close the activation loop.** Add durable destination runs, retries, health, mappings, and bidirectional HubSpot/Salesforce/warehouse sync.
4. **Ship agentic workflows.** Package account research and outbound plays around audiences, signals, workbooks, and MCP.
5. **Add paid-media destinations.** Implemented consent-aware hashed audience sync for Meta, Google and LinkedIn; controlled-live validation remains required before these drivers graduate from beta.
6. **Scale the ecosystem and enterprise layer.** Provider SDK, marketplace packaging, SSO/SCIM, policy controls, and higher-volume execution.

## Definition of parity

A capability only counts when it is tenant-safe, API-addressable, observable, retryable where applicable, tested offline, and validated against a real external system before its release claim changes from beta to supported.
